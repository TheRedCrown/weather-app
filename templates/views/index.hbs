{{>head}}
{{>header}}
    <div class="central">
        <h1>{{title}} pages</h1>
        <p class="para">Welcome to brand new home page!</p>

        <form>
            <input placeholder="Location">
            <button>Submit</button>
        </form>

        <p id="message1"></p>
        <p id="message2"></p>
        {{>footer}}
    </div>
{{>foot}}