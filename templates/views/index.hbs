{{>head}}
{{>header}}
    <div class="central">
        <h1>{{title}} страница</h1>
        <p class="para">Введите название своего города в поле ниже:</p>

        <form>
            <input placeholder="Город, область, страна">
            <button>Подтвердить</button>
        </form>

        <p id="message1"></p>
        <p id="message2"></p>
        {{>footer}}
    </div>
{{>foot}}